# QA Report — 2026-08-16

**Verdict:** No critical findings this run. Sourcing discipline holds up under spot-check — every quantitative claim checked in `report-dataset.md` and this run's newest case files (Credit Suisse/UBS, Pearlman, Rubin) carries a source and confidence tag or an honest `unknown`, every net-assets ratio is labeled UPPER BOUND, and the Vogel afterlife investigation's earlier over-claim ("scoop," "nobody checked") has been thoroughly walked back to hedged, sourced language. The problems are almost entirely repeat findings from the last two audits, still open, plus one new canonical-number concern in `claims-register.md` itself and two new voice/consistency issues in `website.md` and the Notes queue.

---

## CRITICAL

None this run.

---

## SHOULD-FIX

1. **Five pilot-era case files still contradict `report-dataset.md`'s own coding of the same cases — unfixed for a third consecutive audit, and two new violations surfaced this run.** Standard: `case-template.md`'s controlled-vocabulary lists and the binding rule that the coded header must not outrun the narrative; `Cases.base` renders these exact frontmatter fields into the pathway/outcome/decision-owner charts.
   - `cases/terra.md`: `pathway: museum-dissolved` (invalid) · `outcome: collapsed` (invalid) · `decision_owner: unknown` (invalid — should be `no-identifiable-person`). `report-dataset.md` #7 codes this case `pathway: found-standalone-museum`, `outcome: closed-dispersed`, `decision: foundation-executive`.
   - `cases/di-rosa.md`: `pathway: museum+park` (invalid — should be `found-art-park`) · `decision_owner: unknown` (invalid; `report-dataset.md` #9 codes `decision foundation-executive`).
   - `cases/mcnay.md`: `pathway: house-to-museum` (invalid — should be `found-house-museum`) · `verification: in-progress` (invalid — should be `provisional`) · `decision_owner: founder-self` (invalid — should be `collector-alone`, per the template's own McNay worked example).
   - `cases/souls-grown-deep.md`: `decision_owner: unknown` (invalid; `report-dataset.md` #1 codes `foundation-executive`). **New this run:** `outcome: thriving` also contradicts `report-dataset.md` #1's own coding of the same case as `outcome stable` — a second, previously-unflagged field-level contradiction on this file.
   - `cases/neue-galerie.md`: `decision_owner: founder-self` (invalid — should be `collector-alone`, per `report-dataset.md` #3). **New this run:** `pathway: merge-for-perpetuity` is not a `case-template.md` pathway value (should be `merger-into-institution`), and `outcome: pivot` is not a valid `outcome_category` value (should be `pivoted`) — two more invalid values on this file not caught by the last two audits.
   The correct values already exist in `report-dataset.md`/`case-template.md` for all five files; this is still a one-time sync pass, not new research, and it is getting more expensive to defer as more invalid values accumulate on the same files. Recommend doing this before the 50-case set locks.

2. **`marketing/flagship-report.md`'s case-count is now stale by 17 cases, not 14.** It still reads "~30 of 50 cases coded" (line 60), unchanged since 2026-08-14, while `report-dataset.md` — the authoritative live count — now stands at **47 of 50** (2026-08-16, fifth run; three coding batches since the last audit: Kreeger/Michener/Lehman, Enron/Deutsche Bank/Benini/Yuz, Beyeler/Vogel/De la Cruz/JPMorgan, Credit Suisse-UBS/Pearlman/Rubin). Standard: single source of truth — a working page should point to the canonical count rather than restate a number that keeps drifting further from it every audit cycle (gap: 6 → 14 → 17). **Fix:** replace the hardcoded figure with a link to `[[report-dataset]]` rather than a number that needs re-syncing every batch.

3. **`marketing/flagship-report.md` still paraphrases the claims-register's transfer figure as "~$1T" in two places, while correctly using "$992B" in a third.** Lines 6 and 20 read "the ~$1T wave" / "the ~$1T," while line 50 (Standards section) correctly states "$992B." `claims-register.md` C1 is explicit: "do NOT... paraphrase," link instead. Flagged at the last audit as MINOR; re-flagged here as SHOULD-FIX given it is now an internal inconsistency within the same file (one correct figure, two stale paraphrases) rather than a one-off. **Fix:** replace both "~$1T" instances with "$992B" or a link to `[[claims-register]]` C1.

4. **`marketing/website.md`'s "finalized copy" blocks for alinaokun.com still contain em dashes, unresolved for a third consecutive audit.** Lines 48–49 ("Nariway — Independent work on...", "Artobiography — An independent publication...") and lines 55–57 ("The Brief Experiment — A history of...", "Luminary Leadership —", "Meet You in Calabar —") fail the `voice.md`/`ai-tells.md` em-dash ban. The file's own "LIVE revised, confirmed 2026-08-14" note (line 60) still says the actual live page's About-text punctuation was never independently verified ("Could not verify exact About-text punctuation from a paraphrase, worth a quick self-check"). **Fix:** either confirm via screenshot/CMS copy-paste that the live page differs and is clean, or rewrite the vault draft without em dashes and push it live — this shouldn't carry into a fourth audit cycle unresolved.

5. **`marketing/website.md` carries an independent, drifted copy of a biographical fact instead of pointing to `positioning.md`.** The interim nariway.com About copy (line 30) states "She has visited more than 100 museums," while `positioning.md`'s canonical Substack About page (the single source for this fact) says "visited hundreds of museums." Unlike `marketing/linkedin.md` and `marketing/substack.md` — both of which carry an explicit banner pointing all edits to `positioning.md` — `website.md` has no such pointer and independently rewrites the ~20-years-in-finance sentence in three separate places (`positioning.md`'s LinkedIn bio, `website.md`'s nariway.com About, `website.md`'s alinaokun.com Hero) with inconsistent phrasing each time. Standard: single source of truth (QA remit e). **Fix:** add the same "edit only in positioning.md" pointer `linkedin.md` and `substack.md` use, and reconcile the museum-count figure with the canonical text.

6. **All four drafted Notes in `marketing/substack-notes-queue.md` violate `voice.md`'s closing rule before they've gone live.** `voice.md` (Substack Notes section) requires ending "on a concrete fact or image, never a manufactured conclusion," explicitly naming "the summarizing moral" and "the antithesis kicker" as the failure mode to avoid. As drafted: Note 1 ends on an explanatory line ("I wrote about how that changed the way I see them") rather than a fact; Note 2 ends on "Think of it like the difference between a posed photograph and a candid shot," which matches `ai-tells.md` tell #3, the two-picture metaphor with no advice, almost verbatim; Note 3 ends on a sentimental summary ("His story stays with me"); Note 4 ends on an implied CTA ("More on him here"), which also breaks the Notes convention against links in the body. **Fix:** rewrite all four closings to end on the last concrete fact or image, before any of them post.

7. **`research/claims-register.md` C10 may have its own trend direction backwards, and this wasn't caught before the claim entered the register.** C10 states wealth managers offering art services rose "to ~51%, up from roughly 25% in 2011." But the register's own underlying source material — `archive/art_finance_museums_artobiography_chat.md` (lines 39, 131) — describes the *same* 51% figure moving in the opposite direction: down from 63% in 2023 to 51% in 2025. These are two different claims about the same statistic (a 26-point rise over 14 years vs. a 12-point fall over 2 years), and C10 already carries a "confirm exact figure in the report before citing publicly" caveat — this is exactly the kind of check that caveat exists to force, and it hasn't been resolved. Standard: claims-register.md is supposed to be the resolved, single source of truth once a claim is entered; right now it may be citing the wrong direction of its own trend. **Fix:** re-verify against the primary Deloitte Art & Finance report before C10 is used anywhere public-facing (it isn't yet — `report-dataset.md`/`positioning.md` don't cite it), and correct the register text if the archive chat's reading is the accurate one.

---

## MINOR

8. **Field-name mismatch between template and case files persists.** `case-template.md` specifies `verification_status` with capitalized values; case files (including all five in finding 1) still use a lowercase `verification:` field. Harmless — applied consistently — flagged at the last two audits too; worth a normalization pass whenever finding 1's vocabulary sync happens, since the same files are already being touched.

9. **Two more informal "~$1T" paraphrases of the $992B figure, outside `flagship-report.md`.** `crm/partners/Dawn Mari La Monica.md:36` states "the ~$1T art transfer" with no source link. `research/market-intelligence.md:56` cites a Bloomberg headline's "$1 trillion" but immediately ties it back to C1 as "the same claim, rounded" (line 58) — self-correcting, not drift, noted only for completeness. Low-traffic internal files, not public copy; fix opportunistically.

---

**Counts:** 0 critical / 7 should-fix / 2 minor.
