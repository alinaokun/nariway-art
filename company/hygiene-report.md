# Vault Hygiene Report

*Last swept: 2026-08-17, by the [[vault-hygiene|librarian]] cloud routine. Overwritten each run — see git history for prior sweeps.*

## Housekeeping note (not a vault issue, mentioning so it's not a surprise)
Local `main` in this checkout was stale — created from an origin/main state as of 2026-08-13, before origin's history was rewritten upstream (a force-push, per the fetch output). The two histories shared no common ancestor. Reset local `main` to match `origin/main` (the real, currently-pushed state) rather than attempting to merge unrelated histories. No data was at risk; nothing else needed doing.

## Fixed this run (8 commits' worth of safe changes)

- **Broken wikilinks, three CRM notes.** `Mari-Claudia Jimenez.md`, `Mark E. Haranzo.md`, `Diana Wierbicki.md` each linked `[[lawyers|lawyers.base]]` — the link *target* was `lawyers` (no file by that name), only the display text said `.base`. Fixed the target to `lawyers.base`, the actual file.
- **Broken wikilink**, `research/field-definition.md` linked `[[nariway-vision-access|vision]]`, no such note exists or ever did. The referenced idea ("the public benefit must be real, not assumed") is `company/positioning.md`'s own "Why Nariway exists (the vision)" section, so pointed the link there instead of leaving it dangling.
- **INDEX.md, seven missing pointers ("stale map").** Several standing artifacts built over the past week had no line in INDEX: `[[cfo-brief]]` and `[[cmo-brief]]` (the two daily steward-agent briefs), `[[editorial-direction]]` and `[[editorial-candidate-packet]]` (the Artobiography relaunch identity and its story pool), `[[board-opportunities]]` (the researched nonprofit-board list), `[[design-system]]` (the visual house style, sibling to `[[voice]]`), `[[site-health]]` (the Mon/Thu site/SEO watch agent, sibling to vault-hygiene/QA in the on-call line), and `[[institution-building]]` (the section's own charter file, previously only its sub-notes were linked). Added one line each, no restructuring.
- **HOME.md, stale status (dated 16 Aug, read on 17 Aug).** The top action item still described "the last prep day before Query A posts tomorrow" — Query A actually posted live Sunday evening (per HOME's own "Recently" log and the git history), so the page was telling Alina to prep for something already done. Corrected that line and two director-lens mentions ("tomorrow's Qwoted post") to past tense, using only what the file's own "Recently" section already established — no new judgment, just syncing tense to already-recorded fact. Left the "Recently" log itself and the CFO/CMO steward briefs' dated daily entries untouched (those are historical journal entries, not current-state fields; rewriting them would falsify the record).
- **HOME.md, work-log range understated.** Item 2 asked Alina to confirm "Aug 14 and Aug 15" hours, but `work-log.md` shows *four* rows (Aug 12–15) still marked "pending Alina," not two. Corrected the range to match the actual table.
- **HOME.md and flagship-report.md, stale case count.** Both still said "47 of 50" / "~30 of 50" cases coded; `cases/report-dataset.md` (the canonical count) reached **50 of 50** on 2026-08-16 evening. This exact drift was already flagged as a recurring problem in `qa-report.md` (gap grew 6→14→17→20 cases across three prior audits) with an explicit recommended fix — point at `[[report-dataset]]` instead of hardcoding a number that needs re-syncing every batch. Applied that fix in both files.
- **flagship-report.md, paraphrased claim.** Two mentions of "~$1T" for the art-wealth-transfer figure violated `claims-register.md`'s own rule ("when you use one of these, link to it — do not paraphrase"); the canonical figure is $992B (claim C1). Replaced both with a citation/link to C1.
- **Five case files, invalid/drifted frontmatter (`pathway`/`outcome`).** `case-template.md` defines a controlled vocabulary for these fields, and `report-dataset.md` had already computed the correct values during coding; the case files' own frontmatter just hadn't been synced, which breaks `Cases.base`'s pathway/outcome charts. This exact drift was flagged as open across 3 consecutive QA audits. Synced to the already-computed values:
  - `terra.md`: `pathway: museum-dissolved` → `found-standalone-museum`; `outcome: collapsed` → `closed-dispersed`
  - `di-rosa.md`: `pathway: museum+park` → `found-art-park`
  - `neue-galerie.md`: `pathway: merge-for-perpetuity` → `merger-into-institution`; `outcome: pivot` → `merged`
  - `souls-grown-deep.md`: `outcome: thriving` → `stable`
  - `pearlman-foundation.md`: `outcome: dispersed` → `closed-dispersed` (new finding this run, not in prior QA audits)

## Flagged for Alina or Toi (judgment calls, not touched)

1. **`marketing/website.md` vs `company/positioning.md`: a real numeric contradiction, not just a structural risk.** The nariway.com/alinaokun.com bio copy says Alina has visited "more than 100 museums"; the canonical bio in `positioning.md` says "hundreds of museums." Last run flagged this page for holding its own bio copy instead of a pointer as a *latent* risk ("currently agrees... on every fact"); it has now actually drifted. I can't tell which figure is true, that's Alina's own fact, so I didn't touch either. Worth a quick decision: correct one to match the other, and/or finally convert `website.md` to point at `positioning.md` per the established canonical-copy pattern already used in `linkedin.md`/`substack.md`.
2. **`cases/mcnay.md` may be missing from `report-dataset.md`'s master synthesis.** I could not find McNay coded anywhere in `report-dataset.md`, despite `mcnay.md` and `mcnay-packet.md` existing and being actively worked. This is the identical indexing-gap failure mode already caught and fixed twice this month (Batch 6 for 20 cases, Batch 8 for MoMA) — a case can be fully coded and never folded into the master file. I didn't touch `mcnay.md`'s frontmatter (`pathway: house-to-museum`, `status: founder-review`, `verification: in-progress` are also non-standard vocabulary) because there's no canonical value to sync from yet, and folding it into report-dataset is research synthesis work, not a mechanical fix.
3. **Two broken wikilinks to entities with no note of their own**, left alone since creating a note is content creation, not hygiene:
   - `crm/partners/Hugh Meyer.md` links `[[Highline Wealth Partners]]`, his firm, no CRM note exists for it.
   - `crm/partners/Dawn Mari La Monica.md` links `[[Nina Kong-Surtees]]` — flagged in the last two sweeps, still unresolved.
4. **9 of 18 `crm/partners/` notes have no `priority` field** (Christine Anagnos, Diana Wierbicki, Hugh Meyer, John Bacon, Katja Zigerlig, Mari-Claudia Jimenez, Mark E. Haranzo, Matthew F. Erskine, Ruth Flynn Raftery) — `partners.base`'s table/board views sort on it. Mostly recent public-source leads not yet triaged; assigning a priority is a judgment call about relative importance, not something to guess in a batch.
5. **`artobiography/dossier-de-la-cruz.md`'s `related` field looks garbled.** It reads `"[[dossier]] · case: [[de-la-cruz-collection]] · [[editorial-direction]]"` — `[[dossier]]` doesn't resolve to any note, and the sibling file `dossier-vogel.md` uses the parallel-structured `"[[editorial-direction]] · case: [[vogel-collection]] · sibling dossier: [[dossier-de-la-cruz]]"`. Looks like de la Cruz's version should probably read the same way (with a "sibling dossier" pointer to `dossier-vogel`), but that's a guess at intent, not a typo I'm confident correcting myself.
6. **`research/claims-register.md` C10** (51% of wealth managers offering art services) may have its trend direction backwards versus its own underlying source — flagged in `qa-report.md`, still open, already carries a "confirm before citing" caveat in the file itself.
7. **`finance/work-log.md` is missing rows for Aug 16 and Aug 17.** `capital-ledger.md`'s Aug 16 entry already references "Aug 12–16 work-log hours (five days now)," implying a row should exist. I flagged the gap but didn't fabricate an hours estimate — that's Alina's own figure per the file's own rule.
8. **`marketing/website.md`'s "finalized copy" (lines 48–49, 55–57) uses em dashes as label separators** (`Nariway — Independent work on...`), which reads as a `voice.md` violation ("No em dashes. Use commas or restructure.") on a literal reading. This is live public copy, not internal vault prose, and the em-dash-as-label-separator pattern is used throughout the vault's own internal formatting (including `voice.md`'s own title) — genuinely unclear whether the rule was meant to reach this construction. Flagging rather than rewriting already-live website copy on an ambiguous call.

## Otherwise clean
`crm/prospects/` (all 12 files, complete frontmatter, no overdue follow-ups). Spot-checked ~15 other case files against `case-template.md` — all consistently schema'd apart from the five fixed and McNay above. No empty, orphaned, or abandoned-stub files found anywhere outside `archive/`. `institution-building/`, `finance/`, and `learning/` have no duplicate notes — pairs that look similar (`decision-map`/`knowledge-base`, `cfo-brief`/`cfo-brief-2026-08`) are each deliberately distinct and cross-referenced. Substack audience numbers (~434 subscribers, ~1,472 followers) are consistent everywhere they appear. `linkedin.md` and `substack.md` correctly point to `positioning.md` for bio copy, no drift. The old `debriefs/` folder remains gone (archived as a single file), and `archive/` itself is small and intentional (4 items).
