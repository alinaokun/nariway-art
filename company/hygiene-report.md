# Vault Hygiene Report

*Last swept: 2026-08-24, by the [[vault-hygiene|librarian]] cloud routine. Overwritten each run — see git history for prior sweeps.*

## Fixed this run (3 commits' worth of safe changes)

- **`learning/Private Museums and POFs - Erskine (Forbes).md` was silently invisible to `reading-list.base`.** The base filters on `type == "reading"`; this note had `kind: resource` but no `type` field at all, so it never appeared in the reading-list view even though the content was fine. Added `type: reading`, matching every sibling note's schema.
- **`INDEX.md`'s case-library line was stale again.** It still read "~65 coded case notes"; `report-dataset.md` itself now reconciles 80/80 coded case files (confirmed by direct `status: coded` count on disk). Updated to "~80."
- **`INDEX.md` was missing 11 notes created since the last sweep**, none linked from the map: `vault-review`, `cfo-brief-2026-08`, `collection-futures-study`, `pathway-family-proposal-shared-campus`, the five-note Landscape architecture chain (`guide-domain-map` → `landscape-domain-audit` → `landscape-subject-map` → `landscape-ia-stresstest` → `landscape-subject-inventory`), and three artobiography dossiers (`dossier-de-la-cruz`, `dossier-vogel`, `vogel-fifty-states-afterlife`). Added one-line pointers under the right owner section, purely additive — no existing INDEX text changed. All new links verified to resolve.

## Flagged for Alina or Toi (judgment calls, not touched)

1. **`cases/mcnay.md` still isn't folded into `report-dataset.md`'s numbered master synthesis**, while its narrative is cited as supporting evidence for Pattern 1 — unchanged across many consecutive sweeps and QA audits. `mcnay.md` still carries `status: founder-review` and sits outside the numbered dataset. Fold it in, or drop it from Pattern 1 until it clears review — research judgment, not a mechanical fix.
2. **`cases/moma.md` still carries two values outside `case-template.md`'s controlled vocabulary** (`collection_coherence: absorbed`, `decision_owner: collective-founders-trustees`) — a real schema gap (also tracked as QA #17), needs a template amendment or a deliberate remap.
3. **Two broken wikilinks to entities with no note of their own, unchanged from prior sweeps** (creating a full CRM profile is content creation, not a hygiene fix):
   - `crm/partners/Hugh Meyer.md` links `[[Highline Wealth Partners]]`, his firm — no CRM note exists.
   - `crm/partners/Dawn Mari La Monica.md` and `marketing/substack-register.md` link `[[Nina Kong-Surtees]]` (smART Advisory) — flagged for many sweeps running, still unresolved.
4. **`artobiography/dossier-de-la-cruz.md`'s `related` field is still garbled** (`"[[dossier]] · case: [[de-la-cruz-collection]] · [[editorial-direction]]"` — `[[dossier]]` resolves nowhere). Sibling `dossier-vogel.md` uses the working parallel form (`sibling dossier: [[...]]`). Very likely de la Cruz's should mirror it, but that's an inferred fix, not a typo correction.
5. **`crm/partners/` triage gap is unchanged**, not worse: still 22 of 31 partner notes with no `priority` field. No new partner notes landed this window, so the backlog neither grew nor shrank. `partners.base`'s table/board views sort on it — assigning priority is relative-importance judgment, not something to batch-guess.
6. **QA's 2026-08-23 audit has 3 new CRITICALs open** (a misattributed figure in the live public export, two cases with `public_verified: true` on admittedly-unverified facts, and a fabricated buyer quote in `collection-futures-study.md`) plus 12 should-fix items. That's QA's remit (factual/editorial), not hygiene's — flagged here only so it isn't missed if `qa-report.md` itself isn't checked. Not touched.
7. **Worth a look, not urgent:** `company/vault-review.md` is dated 12 August and reads as a snapshot from before the Collection Index / Landscape architecture work existed. Still linked from HOME and now cross-referenced from INDEX (added this run), but its "keep/cut/build" picture is ~12 days and a large amount of work out of date. A fresh pass (or an explicit "superseded, see collection-index-build-brief" note) is a judgment call for whoever owns it next, not something to silently rewrite.

## Note (not a vault-content issue, but worth surfacing)

This run's clone came up with a local `main` several days older than, and with **no common git ancestor to**, `origin/main` — the current remote history (59 commits) contains none of the prior hygiene, QA, or steward commits by name, even though their file outputs (this report, `qa-report.md`, etc.) are still present in content. That means `origin/main`'s commit history was replaced with an unrelated graph at some point since the last sweep — the working tree looks fully intact and consistent, but the git audit trail before this point is gone from this remote unless it survives elsewhere (a fork, a local clone, GitHub's own history). Outside vault-hygiene's remit to fix; flagging so Toi/Alina can confirm it was intentional (e.g. a deliberate history squash) rather than an accidental force-push.

## Otherwise clean

Full-vault wikilink sweep (291 files) found no other broken links beyond the items above (`lawyers.base` and the deliberately-deferred `[[distribution-map-mcnay]]` placeholder in `experiment-ledger.md` are not broken — one's a real `.base` file, the other is explicitly "deferred until an article exists"). No empty files anywhere. Checked every `.base` file's `type ==` filter against its folder's notes (`partners.base`, `prospects.base`, `events.base`, `specialists.base`, `reading-list.base`) — only the one reading-list item above was missing its type. All five `company/decisions/` records are correctly labeled: three decided, one open (`2026-08-14-board-membership.md`, still accurate — `board-opportunities.md` is still actively growing), and the 2026-08-22 daily-check-in retirement decision's "Outcome: to fill in later" is legitimate (due for review in ~3-4 weeks, not yet). `archive/` unchanged (4 items). No retired-practice clutter, no duplicate case/partner/prospect files, no files in the wrong folder.
