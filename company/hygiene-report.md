# Vault Hygiene Report

*Last swept: 2026-08-28, by the [[vault-hygiene|librarian]] cloud routine. Overwritten each run — see git history for prior sweeps.*

## Context for this run
Since the 2026-08-26 sweep, Nariway pivoted (2026-08-27) from the research/advisory model to a professional-services firm for estate attorneys and fiduciaries. `INDEX.md` carries a migration banner marking most of its old-model content as pending rewrite, and `archive/README.md` documents a first archive pass with more "pending decisions and routine changes, so it has not moved yet." Given that, this sweep stayed conservative: small, verifiable, non-structural fixes only. It did not touch anything the migration is already mid-way through — that's Alina/Toi's call, not a mechanical hygiene fix.

Also: this session's local git cache started stale again (same "forced update on fetch" pattern noted in earlier reports) — detached at an old commit while the real `origin/main` tip was 9 commits ahead. Confirmed via `git ls-remote` and fast-forwarded cleanly; nothing was lost, just a local-cache artifact.

## Fixed this run (2 commits' worth of safe changes)

- **`INDEX.md`'s coded-case count was stale.** Read "~92 coded case notes"; direct count of `type: case` frontmatter in `cases/` is now 106 (up from 92 across the Cloud research runs since the last sweep). Updated to "~106."
- **`marketing/events/events-radar.md` had a URL wrapped as a wikilink** — `([[https://www.naepc.org]])`, which Obsidian would try to resolve as a note named "https://www.naepc.org" (it doesn't exist) rather than rendering a clickable link. Converted to a proper markdown link `([naepc.org](https://www.naepc.org))`.

## Flagged for Alina or Toi (judgment calls, not touched)

1. **cfo-steward and cmo-steward are still running despite Alina's explicit 2026-08-27 decision to turn both off** (`company/agent-inventory.md` §7–12). The registry says this was blocked only on missing trigger IDs. I pulled them directly: **cfo-steward = `trig_014KCjwMrTLo85e1sKudTUof`, cmo-steward = `trig_01G6v8M8PYcokUiXRtxfb1TJ`** (both still `enabled: true`, next runs today ~15:03/16:01 UTC). I could not disable them myself — both were created via the web UI (`http_api`), and an agent can only modify routines it created itself. Whoever has browser access to claude.ai/code/routines can now go straight to these two IDs and flip them off, no more ID hunting needed. Until then, both keep committing old-model CFO/CMO content to the vault daily, which is exactly the kind of noise this sweep exists to prevent.
2. **An undocumented cloud routine exists: "Nariway legal review" (`trig_019PtbouQNYWfKFrBCZ6CAmA`), cron `0 13 * * 1` (Mondays), enabled.** It targets the separate `nariway-rebuild`/site repo (keeps `/privacy` and `/terms` current), not this vault, and predates the reset (created 2026-08-15). `agent-inventory.md`'s own governing rule states "No automated routine may exist unless it is documented in this registry" — this one isn't listed anywhere in it. Worth either adding it to the registry (if it should stay) or confirming it's still wanted, since it currently violates the registry's own completeness rule. Not touched — outside this sweep's remit (registry judgment, not a vault file fix) and I have no create/delete rights on it either way.
3. **Two overdue, unresolved partner follow-ups**: `crm/partners/Judith Prowda.md` and `crm/partners/Rebecca L. Fine.md` both carry `next_followup: 2026-08-26` (now 2 days past) with `last_contacted: ""` (never yet contacted) and `stage: warm`. This is exactly the "a 'waiting on X' that already happened" pattern the sweep watches for, but whether the date should move, the stage should change, or the outreach is simply overdue is Alina's call, not a mechanical correction.
4. **`company/decisions/2026-08-14-board-membership.md` is still marked open** ("shortlist being researched into `board-opportunities`"), from before the estate-attorney pivot. The board criteria it lists (arts/culture boards, collector-heavy rooms) predate the new positioning; whether board pursuit is still the right move, or this decision should be revisited/closed given the pivot, is a judgment call for Alina, not something to silently reinterpret.
5. **`archive/README.md` and `company/agent-inventory.md` both link `[[strategic-pivot-2026-08]]`, calling it "the memory file (the summary of the shift)" — but no vault note by that name exists anywhere**, and the phrase "memory file" suggests it may refer to something outside the vault (e.g. an AI memory/session summary) rather than an Obsidian note. Either way, both wikilinks currently resolve to nothing inside Obsidian. Not a mechanical fix: writing the actual pivot summary is content creation, and I can't tell if "memory file" was meant to be a vault note at all. Flagging so it doesn't stay a silently broken link.
6. **The old-model daily briefs are still sitting in their usual places, now describing retired routines**: `company/scoreboard.md` and `company/weekly-brief.md` (both fed by the weekly-advisor routine, disabled 2026-08-27) and `finance/cfo-brief.md` / `marketing/cmo-brief.md` (fed by cfo-steward/cmo-steward, decided-off per item 1 above but still running). These read as current-tense ("maintained by the cfo-steward cloud agent") but describe machinery Alina has already moved past. `archive/README.md` itself says this cluster is "pending decisions and routine changes" and hasn't moved yet — flagging as a natural candidate for the next archive pass once items 1 and the broader CMO/org file decisions land, not acting on it now since it's explicitly mid-migration.

## Otherwise clean

Full-vault wikilink sweep (349 files) found no *new* broken links beyond the two fixed above. Confirmed as non-issues (regex false positives / known-good): `[[wikilink]]`/`[[wikilinks]]` prose mentions in `report-dataset.md` and `vault-hygiene.md`; `lawyers.base` links (the `.base` file exists, my link-checker only indexes `.md` notes); the escaped `\|` pipes inside `gwt-storyboard.md`'s markdown table (all 8 targets — `storm-king`, `barnes`, `fisher-sfmoma`, `chinati`, `souls-grown-deep`, `anderson-collection-stanford`, `de-la-cruz-collection`, `vogel-fifty-states-afterlife` — verified to exist); the deliberately-deferred `[[distribution-map-mcnay]]` placeholder in `experiment-ledger.md`; `company/design-system.md`'s link to `prototypes/gwt-page` (an existing `.html` prototype file, not a missing note). No empty or near-empty files anywhere.

Unchanged from prior sweeps, still genuinely broken, still flagged rather than fixed (no note exists to point to, and creating one is content creation): `crm/partners/Hugh Meyer.md` → `[[Highline Wealth Partners]]`; `crm/partners/Dawn Mari La Monica.md` → `[[Nina Kong-Surtees]]`; `artobiography/dossier-de-la-cruz.md`'s `related` field still carries a dangling `[[dossier]]` (sibling `dossier-vogel.md`'s `sibling dossier: [[...]]` pattern is the likely intended fix, inferred not confirmed).

All `.base` filters checked against their folders: 106/106 `type: case` files in `cases/`, 43/43 `type: partner` in `crm/partners/`, 15/15 `type: prospect` in `crm/prospects/`, 12/12 `type: specialist` in `institution-building/specialists/`, 26/26 `type: event` in `marketing/events/`, 29/29 `type: reading` in `learning/` — full coverage, no missing frontmatter, nothing silently excluded, nothing in the wrong folder. The large web of `[[wikilinks]]` from live vault files into the newly-archived `research/`, `finance/`, and `institution-building/` notes (e.g. `field-definition`, `problem-discovery`, `institution-building`) still resolve fine — Obsidian links by filename, not folder, so moving a note to `archive/` doesn't break the link, it just leaves old-model cross-references live until those referencing files are themselves archived or rewritten, which `archive/README.md` already flags as pending. All five `company/decisions/` records remain correctly labeled: three decided-and-closed, `2026-08-22-retire-daily-checkin.md` decided (though its described routine has since been retired again, see flag 6), and `2026-08-14-board-membership.md` genuinely still open (flag 4).
